import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tabBar: {
    shadowColor: "#fff",
    shadowOffset: {
      width: 0,
      height: 0,
    },
    shadowOpacity: 0.0,
    elevation: 0,
    borderTopColor: "#ebebeba7",
    borderTopWidth: 1,
  },
});
